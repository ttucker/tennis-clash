{
  "buildCommand": "npm run build",
  "installCommand": "npm install --no-audit --no-fund",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/tennis-clash" }
    }
  ]
}
